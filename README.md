# setup
Base setup script - removed ansible dependencies

run specific task with `./run "script"`, or dry run with `./run--dry "script"` eg. `./run --dry dev`.

for new pc install run `./arch-new-install`
